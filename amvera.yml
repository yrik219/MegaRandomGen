meta:
  environment: node
  toolchain:
    name: npm
    version: "22"
build:
  artifacts:
    "*": /
  additionalCommands: npm run build
run:
  scriptName: main.js
  command: npm run startp
  persistenceMount: /data
  containerPort: 80
